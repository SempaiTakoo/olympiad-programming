#include <bits/stdc++.h>

using namespace std;
using ull = unsigned long long;

void solve() {
    int n;
    cin >> n;
    vector<int> vec(n);
    for (int i = 0; i < n; ++i) {
        cin >> vec[i];
    }
    bool isSorted = false;
    int count = 0;
    while (!isSorted) {
        
    }
}

int main() {
    int t;
    cin >> t;
    while (t--) {
        solve();
    }
}