#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;
using ll = long long int;

ll MIN = -9223372036854775808;

void solve() {
    int n;
    cin >> n;
    vector<ll> vec(n);
    for (int i = 0; i < n; ++i) {
        cin >> vec[i];
    }
}

int main() {
    int t;
    cin >> t;
    for (int i = 0; i < t; ++i) {
        solve();
    }
}